// Declaring a variable to use in out loop's boolean expression
int number = 0;

//While the value in number is less than or equal to 1000, run the fillowing
while (number <= 100)
{
    //write the value of number to the console and then increment by 5
    Console.WriteLine(number);
    number = number +5;
}